import React from "react";
import {
  Animated,
  Dimensions,
  FlatList,
  Image,
  StyleSheet,
  Text,
  View,
} from "react-native";
import { COLORS, countries } from "../../constants/index";

const Countries = () => {
  return <View style={styles.continer}></View>;
};

export default Countries;

const styles = StyleSheet.create({
  continer: {},
});
